// taken from Grain's guide https://grain-lang.org/docs/guide/functions#Recursive-Functions
let rec fib = (n) => {
  if (n == 0 || n == 1) {
    1
  } else {
    fib(n - 1) + fib(n - 2)
  }
}

let x = fib(38)
